node_modules/
dist/

# .env.test is intentionally NOT ignored — it contains only fake test credentials
# and must be committed so npm test works without any setup
.env
.env.local
.env.*.local
.env.production
.DS_Store
