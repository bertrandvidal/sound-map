node_modules/
dist/
coverage/

# Superpowers visual-companion / brainstorm scratch (transient HTML mockups)
.superpowers/

# Env files: ignore all real env files. .env.example (template) is
# intentionally committed.
.env*
!.env.example

.DS_Store

# Vercel CLI project link (vercel link / vercel dev)
.vercel
